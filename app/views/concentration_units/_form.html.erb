<%= simple_form_for(@concentration_unit) do |f| %>
  <%= render partial:  "application_partials/show_form_error_messages", locals: {:model_obj => @concentration_unit } %>

  <%= f.input :name %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn-primary" %>
  </div></br>
<% end %>
