<header>
  <h1>Editing Batch Item</h1>
</header>

<%= simple_form_for(@batch_item) do |f| %>
  <%= render partial:  "application_partials/show_form_error_messages", locals: {:model_obj => @batch_item } %>
  <%= render partial: 'form', locals: {f: f} %>
  <%= render partial: "application_partials/submit", locals: {f: f} %>
<% end %>

<%= link_to 'Back', batch_items_path %>
