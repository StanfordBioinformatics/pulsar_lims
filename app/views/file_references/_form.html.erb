	<%= render partial:  "application_partials/show_form_error_messages", locals: {:model_obj => @file_reference } %>

	<%= f.association :data_storage, label_html: {"data-toggle": "tooltip", "title": "The bucket or project in which this file is stored."} %>
	<%= f.association :data_file_type %>
	<%= f.input :file_path, label_html: {"data-toggle": "tooltip", "title": "The path within the bucket or project. If the referenced data storage object has a base folder path specified, then this path must be specified as a relative (subpath) of the base path."}  %>
	<%= f.input :fileid %>
