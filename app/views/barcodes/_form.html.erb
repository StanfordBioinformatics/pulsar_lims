<%= simple_form_for(@barcode) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :sequence %>
  <%= f.association :sequencing_library_prep_kit %>

  <div class="form-actions">
    <%= f.button :submit, class: "btn-primary" %>
  </div></br>
<% end %>
