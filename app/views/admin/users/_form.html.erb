<%= simple_form_for [:admin, user] do |f| %>
	<%= f.input :email %>
	<%= f.input :password %>
	<%= f.input :role, collection: User::ROLES, prompt:  "Select the user role" %>

	<%= f.button :submit, class: "btn btn-primary" %>
<% end %>
