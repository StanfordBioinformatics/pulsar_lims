<header>
  <h1><%= @cloning_vector.name %></h1>

  <ul class="actions">
    <%= render partial: "application_partials/auth_edit_record", locals: {record: @cloning_vector} %>
    <%= render partial: "application_partials/auth_destroy_record", locals: {record: @cloning_vector} %>
  </ul>
</header>

<%= render partial: "application_partials/record_meta", locals: { record: @cloning_vector } %>

<table class="attributes">
<tr>
  <th><strong>User:</strong></th>
  <td><%= @cloning_vector.user %></td>
</tr>

<tr>
  <th><strong>Name:</strong></th>
  <td><%= @cloning_vector.name %></td>
</tr>

<tr>
  <th><strong>Vendor:</strong></th>
  <td><%= link_to @cloning_vector.vendor.name, @cloning_vector.vendor %></td>
</tr>

<tr>
  <th><strong>Vendor product identifier:</strong></th>
  <td><%= @cloning_vector.vendor_product_identifier %></td>
</tr>

<tr>
  <th><strong>Description:</strong></th>
  <td><%= @cloning_vector.description %></td>
</tr>

<tr>
  <th><strong>Product url:</strong></th>
  <td><%= @cloning_vector.product_url %></td>
</tr>
<tr>
	<th><strong>Map</strong></th>
	<% if @cloning_vector.map.present? %>
		<td><%= image_tag @cloning_vector.map, alt: "Map", class: "map-img" %></td>
	<% else %>
		<td></td>
	<% end %>
</tr>
</table>

<%= link_to 'Back', cloning_vectors_path %>
