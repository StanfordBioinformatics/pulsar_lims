<header>
  <h1 class="index-page-header">Library Fragmentation Methods</h1>
	<p class="model-definition"><%= LibraryFragmentationMethod::DEFINITION %></p>
  
  <ul class="actions">
    <%= render partial: "application_partials/auth_new_record", locals: {model_class: LibraryFragmentationMethod} %>
  </ul> 
</header>

<table class="attributes index">
  <thead>
    <tr>
      <%= render partial: "application_partials/index_common_table_headers" %>
      <th>Description</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @library_fragmentation_methods.each do |rec| %>
      <tr>
        <%= render partial: "application_partials/index_common_table_cells", locals: {record: rec} %>
        <td><%= rec.description %></td>
        <td>
					<%= link_to fa_icon("pencil"), edit_library_fragmentation_method_path(rec) %>
        	<%= link_to fa_icon("trash"), rec, method: :delete, data: { confirm: 'Are you sure?' } %>
				</td>
      </tr>
    <% end %>
  </tbody>
</table>
