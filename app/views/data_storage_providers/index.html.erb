<header>
  <h1 class="index-page-header">                                                                       
    <%= @title || "Data Storage Providers" %>                                                                      
  </h1>
  <p class="model-definition"><%= DataStorageProvider::DEFINITION %></p>

  <ul class="actions">
    <%= render partial: "application_partials/auth_new_record" %>
  </ul> 
</header>

<table class="attributes index">
  <thead>
    <tr>
      <% @model_class = DataStorageProvider %>
      <%= render partial: "application_partials/index_common_table_headers" %>
      <th>Bucket storage?</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @records.each do |rec| %>
      <tr>
        <%= render partial: "application_partials/index_common_table_cells", locals: {record: rec} %>
        <td><%= rec.bucket_storage? %></td>
        <td>
          <%= link_to fa_icon("pencil"), edit_data_storage_provider_path(rec) %>
          <%= link_to fa_icon("trash"), rec, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<% if defined?(@page) %>                                                                               
  <%= paginate @records %>                                                                             
  <%= link_to "All", data_storage_providers_path(all: true) %>                                                        
<% end %>
