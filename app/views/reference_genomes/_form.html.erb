<%= simple_form_for(@reference_genome) do |f| %>
	<%= f.error_notification %>

  <%= f.input :name %>
  <%= f.input :url %>

  <div class="actions">
    <%= f.button :submit, class: "btn-primary"%>
  </div></br>
<% end %>
