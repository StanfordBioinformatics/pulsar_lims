<%= simple_form_for(@reference_genome) do |f| %>
  <% if @reference_genome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reference_genome.errors.count, "error") %> prohibited this reference_genome from being saved:</h2>

      <ul>
      <% @reference_genome.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :name %>
  <%= f.input :url %>

  <div class="actions">
    <%= f.button :submit, class: "btn-primary"%>
  </div></br>
<% end %>
