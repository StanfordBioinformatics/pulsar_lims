<header>
  <h1 class="index-page-header">                                                                       
    <%= @title || "Organisms" %>                                                                      
  </h1>
  <p class="model-definition"><%= Organism::DEFINITION %></p>

  <ul class="actions">
    <%= render partial: "application_partials/auth_new_record" %>
  </ul> 
</header>

<table class="attributes index">
  <thead>
    <tr>
      <%= render partial: "application_partials/index_common_table_headers" %>
      <th>Scientific name</th>
      <th>NCBI Taxon</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @records.each do |rec| %>
      <tr>
        <%= render partial: "application_partials/index_common_table_cells", locals: {record: rec} %>
        <td><%= rec.scientific_name %></td>
        <td><%= rec.ncbi_taxon %></td>
        <td>
          <%= link_to fa_icon("pencil"), edit_organism_path(rec) %>
          <%= link_to fa_icon("trash"), rec, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<% if defined?(@page) %>                                                                               
  <%= paginate @records %>                                                                             
  <%= link_to "All", organisms_path(all: true) %>                                                        
<% end %>
