<!-- Form parameters:
	1) f - The form object to add this partial to. 
	2) documents - an array of docuemnts already associated with the object in question, i.e. a Library or Biosample, 
								 so that a default can be selected from it in the select tag. If empty, will be set to nil and the 
                 prompt will be the default selection.
-->
		             
<%= f.input :documents, label: "Add Protocol Documents From List:", collection: sorted_document_name_id(), selected: selected, prompt: "Select a Document", input_html: {multiple: true} %>

<!--  f.select :documents, choices=options_for_select(sorted_document_name_id,selected=selected),options={},html_options={:prompt => "Select     a Document",:multiple => true}  -->
