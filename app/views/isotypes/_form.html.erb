<%= form_for(@isotype) do |f| %>
  <% if @isotype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@isotype.errors.count, "error") %> prohibited this isotype from being saved:</h2>

      <ul>
      <% @isotype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
