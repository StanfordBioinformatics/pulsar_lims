<%= simple_form_for(@uberon) do |f| %>
	<%= f.error_notification %>

  <%= f.input :name %>

  <%= f.input :accession %>

  <div class="actions">
    <%= f.button :submit, class: "btn-primary" %>
  </div></br>
<% end %>
