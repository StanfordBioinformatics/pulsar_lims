
<!--
Adds the table header fields that are common to each table in the index views.
This partial should mirror the one that has the td values in _index_common_table_cells.html.erb.
-->

<th>Record ID</th>

<!-- @model_class is set in the controller's index action. -->
<% if @model_class.column_names.include?("upstream_identifier") %> 
  <th>Upstream ID</th>
<% end %>

<% if @model_class.column_names.include?("name") %> 
 <th>Name</th>
<% end %>

<th>Creator</th>
<th>Last updated</th>
<th>Date created</th>
