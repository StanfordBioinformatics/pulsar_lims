
<!--
Adds the table header fields that are common to each table in the index views.
This partial should mirror the one that has the td values in _index_common_table_cells.html.erb.
-->

<% no_name = false %>
<% if not local_assigns[:no_name].nil? %>
  <% no_name = true %>
<% end %>

<th>Record ID</th>
<% unless no_name %>
 <th>Name</th>
<% end %>
<th>Creator</th>
<th>Date created</th>
